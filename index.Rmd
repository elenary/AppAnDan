--- 
title: "Прикладной анализ данных"
author: "Елена Рыбина"
date: "`r Sys.Date()`"
site: bookdown::bookdown_site
documentclass: book
bibliography:
- book.bib
- packages.bib
description: |
  Занятие по прикладному анализу данных.
link-citations: yes
github-repo: elenary/AppAnDan
---
```{r, eval=TRUE,echo=FALSE}
library(tidyverse)
library(viridis)
```
# Чек-лист для работы с данными

1. Сформулированы теоретические гипотезы
2. Сформулированы эмпирические гипотезы
3. Есть понимание, как будут выглядеть / выглядят собранные данные
4. Есть понимание, что данных достаточно для проверки поставленной гипотезы
5. Исходя из гипотезы и того, как собраны и какие данные, выбран статистический метод проверки экспериментальной гипотезы
6. Исходя из статистического метода, есть понимание о статистической гипотезе *(хорошо, если есть)*
7. Выбрана среда для анализа данных и пакет для этого метода и конкретная функция
8. Данные приведены в вид, к которому можно применить выбранный статистический метод
9. Визуализация самих данных для лучшего их понимания
10. Проведен расчет выбранного стат метода
11. Вид полученных результатов понятен для интерпретации, и резульатты согласуются с представлением о том, как они должны выглядеть
12. Результаты проинтепретированы и стедлан вывод относительно изначальной гипотезы
13. Визуализированы результаты, так, чтобы их было легче правильно проинтерпретировать


## Сформулированы теоретические гипотезы 

Возьмем датасет с kaggle <https://www.kaggle.com/datasets/uciml/student-alcohol-consumption>
```{r, eval=TRUE}
studens1 <- read_csv("student-mat.csv")
View(studens1)
```

```{r, eval=TRUE}
studens1 %>% 
  rename_with(., ~ paste0(., "_mat"), .cols = c(absences, paid, G1, G2, G3)) -> studens1
```

```{r, eval=TRUE}
studens2 <- read_csv("student-por.csv")
studens2 %>% 
  rename_with(., ~ paste0(., "_por"), .cols = c(absences, paid, G1, G2, G3)) -> studens2
View(studens2)
```


Each **bookdown** chapter is an .Rmd file, and each .Rmd file can contain one (and only one) chapter. A chapter *must*
The `index.Rmd` file is required, and is also your first book chapter. It will be the homepage when you render the book.


## Сформулированы эмпирические гипотезы

Объединенные таблички


```{r, eval=TRUE}

studens1 %>% 
  full_join(studens2, by = c("school","sex","age","address","famsize","Pstatus","Medu","Fedu",
                             "Mjob","Fjob","reason", "guardian", "traveltime","studytime", "failures", "schoolsup", "famsup",
                             "activities", "nursery", "higher", "internet", "romantic", "famrel", "freetime", "goout", 
                             "Dalc", "Walc", "health")) -> students
```

```{r, eval=TRUE}
#kable(studens)
```


## Есть понимание, как будут выглядеть / выглядят собранные данные

## Исходя из гипотезы и того, как собраны и какие данные, выбран статистический метод проверки экспериментальной гипотезы
## Исходя из статистического метода, есть понимание о статистической гипотезе *(хорошо, если есть)*
## Выбрана среда для анализа данных и пакет для этого метода и конкретная функция
## Данные приведены в вид, к которому можно применить выбранный статистический метод
## Визуализация самих данных для лучшего их понимания
## Проведен расчет выбранного стат метода
## Вид полученных результатов понятен для интерпретации, и резульатты согласуются с представлением о том, как они должны выглядеть
## Результаты проинтепретированы и стедлан вывод относительно изначальной гипотезы
## Визуализированы результаты, так, чтобы их было легче правильно проинтерпретировать

<!--chapter:end:index.Rmd-->
